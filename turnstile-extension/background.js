// Turnstile Bypass Extension - Background Service Worker
// This is a minimal background script required for MV3
chrome.runtime.onInstalled.addListener(() => {
  console.log('Turnstile Bypass extension installed');
});
