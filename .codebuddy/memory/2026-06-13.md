# 2026-06-13 Turnstile破解 — 最终成功

## ✅ 破解成功！2026-06-12 23:50

**成功方案**: 真实系统Chrome + Chrome扩展(MAIN world + all_frames) + CDP Shadow DOM穿透点击

**关键发现**: Playwright自带Chromium被Cloudflare识别为自动化。必须用真实Chrome。

**运行文件**: `run_chrome.mjs` + `ext-crack/` 扩展目录

## 详细分析

## 实跑分析结论

经过v4-v9共10+轮实跑测试，核心发现：

1. **Turnstile widget 类型**: Managed/Invisible 型，非简单checkbox
2. **Widget 位置**: Shadow DOM深处，iframe在 `div::shadow > #document-fragment > iframe`，页面坐标 (565,324) 300x65
3. **点击验证**: CDP可以获取坐标，页面点击能到达widget，但**token永远不生成** — `turnstile.getResponse()` 始终返回空
4. **根本原因**: Cloudflare后端的JavaScript挑战判定浏览器为自动化环境，静默拒绝
5. **尝试过的方案**: Shadow DOM穿透、CDP坐标点击、持久化Profile、指纹补丁(v3)、多点点击 — 均无法通过

## 最终产出文件
- `TURNSTILE_ANALYSIS_REPORT.md` — 完整分析报告
- `crack_turnstile_final.mjs` — 最终优化版破解脚本
- 所有测试日志在 `logs/crack_v*` 和 `logs/bare_test` 目录

## 关键脚本差异
- `crack_turnstile.mjs` (v4) — 初始版，一直点"Continue in browser"导致从未分析Turnstile
- `crack_turnstile_v5.mjs` — 移除Continue in browser，加入等待和真人模拟
- `crack_turnstile_v6.mjs` — CDP穿透Shadow DOM找到iframe
- `crack_turnstile_v7.mjs` — CDP Shadow Host + Box Model获取坐标（首次成功获取widget坐标）
- `crack_turnstile_v8.mjs` — 多点点击 + token观察模式
- `crack_turnstile_v9.mjs` — 持久化Profile

## 用户三项洞察的验证
1. ✅ 进入链接后需要等待 — 正确，Turnstile加载需要8-12秒
2. ✅ 模拟真人操作 — 关键但不够，Cloudflare检测数百维度
3. ✅ 不点"Continue in browser" — 正确，该按钮是误导
